# 김비서 대시보드 — 워크숍 재현 가이드

> Claude Code(데스크톱)로 AI 비서 대시보드를 처음부터 끝까지 만드는 전체 과정.  
> 이 파일을 Claude Desktop에 넣고 **"이 가이드대로 처음부터 만들어줘"** 라고 하면 동일하게 재현 가능.

---

## 환경 준비 (최초 1회)

### macOS (Mac Mini 포함)
```bash
# 1. Homebrew 설치 (이미 있으면 생략)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. 필수 CLI 도구 설치
brew install gh                        # GitHub CLI
brew install supabase/tap/supabase     # Supabase CLI
brew install libpq                     # psql (DB 연결용)

# 3. Node.js 설치 (없으면)
brew install node

# 4. Vercel CLI 설치
npm i -g vercel

# 5. 각 서비스 로그인
gh auth login              # GitHub — 브라우저 열림 → Authorize
supabase login             # Supabase — 브라우저 열림 → Authorize
vercel login               # Vercel — 브라우저 열림 → Continue with GitHub
```

### Windows
```powershell
# Winget으로 설치
winget install GitHub.CLI
winget install Supabase.CLI
winget install OpenJS.NodeJS

# npm으로 Vercel
npm i -g vercel

# 로그인 (동일)
gh auth login
supabase login
vercel login
```

---

## 프로젝트 폴더 구조

```
워크숍-실습 4/                  ← 프로젝트 루트
│
├── index.html                  # 소개 메인 페이지
├── dashboard.html              # 업무 대시보드 (핵심)
├── chart.html                  # 매출 차트
├── diagram.html                # 업무 프로세스 래퍼
├── diagram.svg                 # 업무 프로세스 다이어그램
├── meeting-result.html         # 회의록
├── report.html                 # 사이트 분석 보고서
├── manifest.json               # PWA 설정
├── sw.js                       # Service Worker (오프라인)
├── CLAUDE.md                   # 프로젝트 문서
├── .gitignore
├── .env.local                  # 환경변수 (GitHub에 올라가지 않음)
│
├── icons/                      # PWA 아이콘
│   ├── icon.svg
│   ├── icon-192.png
│   └── icon-512.png
│
├── 김비서-데이터/              # 원본 데이터 (gitignore, Supabase에 저장)
│   ├── 업무목록.csv
│   ├── 주간일정.txt
│   ├── 프로젝트현황.csv
│   ├── 매출데이터.csv
│   └── 회의록.txt
│
├── 일기/                       # 일기 파일 (gitignore)
│   └── YYYY-MM-DD.md
│
├── 정리해줘/                   # 문서 정리 예시 (gitignore)
│   ├── 보고서/
│   ├── 메모/
│   ├── 업무/
│   └── 기타/
│
├── supabase/                   # Supabase 설정
│   └── config.toml
│
└── .claude/
    └── commands/
        ├── 김비서.md           # /김비서 슬래시 커맨드
        └── 배포.md             # /배포 슬래시 커맨드
```

---

## 단계별 진행 과정

### STEP 1 — 데이터 파일 준비

`김비서-데이터/` 폴더에 아래 파일을 준비한다.

**업무목록.csv**
```
업무,우선순위,상태,담당자,마감일,카테고리
3월 프로모션 기획안 작성,높음,진행중,김대리,2026-03-12,마케팅
...
```

**주간일정.txt** — 월~금 시간별 일정을 자유롭게 작성

**프로젝트현황.csv**
```
프로젝트명,진행률,상태,담당자,시작일,마감일,예산(만원),집행(만원)
봄맞이 프로모션,65,진행중,김대리,...
```

**매출데이터.csv**
```
날짜,제품,카테고리,수량,단가,매출액,지역
2026-01-05,무선 이어폰,전자기기,45,89000,4005000,서울
```

**회의록.txt** — 자유 형식 텍스트

---

### STEP 2 — index.html (소개 페이지)

**Claude에게:** `"데이터 파일을 읽고 index.html 소개 페이지를 만들어줘"`

핵심 요소:
- 큰 제목 "김비서를 소개합니다"
- 3가지 주요 기능 카드 (업무관리 / 일정정리 / 성과요약)
- [내 대시보드 보기] 버튼 → `dashboard.html`
- **모든 경로는 상대경로** (`/` 로 시작하지 않음 — Vercel 배포 시 404 방지)

---

### STEP 3 — dashboard.html (업무 대시보드)

**Claude에게:** `"데이터를 읽어서 2×2 카드 레이아웃 대시보드를 만들어줘"`

4개 섹션:
1. **할 일 목록** — 체크박스, 우선순위 색상 (높음=빨강, 보통=노랑, 낮음=초록)
2. **이번 주 일정** — 월~금 5컬럼 표
3. **프로젝트 진행률** — 프로그레스 바
4. **매출 요약** — 숫자 카드 + 카테고리 바

상단 "← 메인" 링크 → `index.html`

---

### STEP 4 — 프리미엄 디자인 업그레이드

**Claude에게:** `"글래스모피즘, 그라디언트 배경, 다크/라이트 모드 토글 추가해줘"`

- 글래스모피즘: `backdrop-filter: blur(28px)` + 반투명 배경
- 그라디언트 배경 + 컬러 오브(blob) 3개
- 다크 모드: 배경 `#0f0f23`, 카드 `rgba(255,255,255,0.05)`, 텍스트 `#e0e0e0`
- localStorage 키 `'kim-theme'` 으로 양쪽 페이지 공유
- `<head>`에 즉시 실행 스크립트로 FOUC(깜빡임) 방지

---

### STEP 5 — 추가 페이지들

```
chart.html          — 매출 차트 (순수 SVG, 외부 라이브러리 없음)
meeting-result.html — 회의록 요약 + 액션 아이템 표 + 인쇄 버튼
diagram.svg         — 기획→제작→검토→배포→분석 5단계 SVG
diagram.html        — diagram.svg 래퍼 (네비게이션 포함)
report.html         — 사이트 분석 보고서 (카드 레이아웃)
```

**chart.html 핵심:** 외부 라이브러리 없이 SVG로 선 그래프 + 막대 그래프 구현, `window.resize`로 반응형

---

### STEP 6 — 상단 탭 네비게이션

**Claude에게:** `"모든 페이지에 탭 메뉴 추가해줘. 현재 페이지 강조"`

```html
<!-- 각 HTML 파일 <body> 바로 아래 -->
<nav class="tab-nav">
  <a href="dashboard.html"      class="tab-item">🏠 대시보드</a>
  <a href="meeting-result.html" class="tab-item">📋 회의록</a>
  <a href="chart.html"          class="tab-item">📊 매출 현황</a>
  <a href="diagram.html"        class="tab-item">🔄 업무 프로세스</a>
  <a href="report.html"         class="tab-item">🔍 사이트 분석</a>
</nav>
```

JS로 현재 페이지 감지:
```javascript
const page = location.pathname.split('/').pop();
document.querySelectorAll('.tab-item').forEach(a => {
  if (a.getAttribute('href') === page) a.classList.add('active');
});
```

---

### STEP 7 — 반응형 대응

**Claude에게:** `"dashboard.html을 PC·태블릿·모바일 반응형으로 만들어줘"`

브레이크포인트:
- `@media (max-width: 1024px)` — 태블릿: 패딩 축소
- `@media (max-width: 640px)` — 모바일: 1열 그리드, 날짜/담당자 숨김
- `@media (max-width: 380px)` — 소형: 추가 축소
- 일정 테이블: `<div class="schedule-wrap">` 래퍼로 가로 스크롤

---

### STEP 8 — PWA (앱 설치 가능)

**Claude에게:** `"이 사이트를 PWA로 만들어줘. 앱 이름: 김비서"`

필요한 파일:
1. `manifest.json` — 앱 이름, 아이콘, 시작URL, shortcuts
2. `sw.js` — Service Worker (캐시 우선 전략)
3. `icons/icon.svg` — SVG 아이콘 ("김" 글자 + 보라색 배경)
4. `icons/icon-192.png` / `icon-512.png` — PNG 아이콘 (Python으로 생성)

각 HTML 파일 `<head>`에 추가:
```html
<link rel="manifest" href="manifest.json">
<meta name="theme-color" content="#6366f1">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-title" content="김비서">
<link rel="apple-touch-icon" href="icons/icon-192.png">
```

각 HTML 파일 `</body>` 직전에 추가:
```html
<script>
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', () => navigator.serviceWorker.register('sw.js'));
  }
</script>
```

---

### STEP 9 — 일기 기능

**Claude에게:** `"일기 쓸 수 있게 해줘. 일기 폴더에 저장하고 대시보드에 리스트 보이게"`

- `일기/YYYY-MM-DD.md` 형식으로 저장
- dashboard.html 하단에 전체 너비 `📔 나의 일기` 카드 추가
- 클릭하면 전문 펼치기/접기

---

### STEP 10 — 슬래시 커맨드 만들기

**Claude에게:** `"/김비서 슬래시 커맨드 만들어줘"`

파일 위치: `.claude/commands/김비서.md`

```markdown
---
description: 김비서-데이터 읽고 대시보드 업데이트 후 오늘 브리핑 출력
---

## STEP 1 — 데이터 읽기
김비서-데이터/ 폴더의 5개 파일을 모두 읽어줘.

## STEP 2 — dashboard.html 업데이트
읽은 데이터로 tasks, projects, diaries를 최신 상태로 동기화해줘.

## STEP 3 — 오늘의 브리핑
오늘 날짜 기준으로 일정, 우선순위 할 일, 프로젝트 현황, 매출, 액션 아이템을 정리해줘.
```

**Claude에게:** `"/배포 슬래시 커맨드 만들어줘. 변경 파일 확인→커밋→푸시→완료 메시지"`

파일 위치: `.claude/commands/배포.md`

---

### STEP 11 — GitHub 설정

```bash
# 1. GitHub 로그인
gh auth login   # Continue with GitHub → 브라우저 → Authorize

# 2. 프로젝트 폴더에서 git 초기화
cd "프로젝트 폴더"
git init
git branch -M main

# 3. .gitignore 생성
# 아래 내용 포함:
# .DS_Store, Thumbs.db, *.log
# .env.local, .env*.local
# 김비서-데이터/, 일기/, 정리해줘/, supabase/migrations/

# 4. GitHub 레포지토리에 연결 (레포는 GitHub에서 미리 생성)
git remote add origin https://github.com/[계정명]/[레포명].git

# 5. 첫 커밋 & 푸시
git add .
git commit -m "초기 배포"
git push -u origin main
```

---

### STEP 12 — Supabase 설정 (데이터 클라우드 저장)

**목적:** CSV·TXT 데이터 파일을 GitHub에 올리지 않고 Supabase DB에 저장

```bash
# 1. Supabase CLI 설치 & 로그인
brew install supabase/tap/supabase
supabase login   # 브라우저에서 로그인

# 2. 프로젝트에서 연결할 Supabase 프로젝트 확인
supabase projects list

# 3. 프로젝트 폴더에서 초기화 & 링크
cd "프로젝트 폴더"
supabase init
supabase link --project-ref [PROJECT_REF]
```

**테이블 생성 SQL** (Supabase 대시보드 SQL 에디터에서 실행):
```sql
CREATE TABLE IF NOT EXISTS kimbiseo_tasks (
  id SERIAL PRIMARY KEY, name TEXT, priority TEXT,
  status TEXT, assignee TEXT, due_date TEXT, category TEXT
);
CREATE TABLE IF NOT EXISTS kimbiseo_schedules (
  id SERIAL PRIMARY KEY, day TEXT, time TEXT, title TEXT
);
CREATE TABLE IF NOT EXISTS kimbiseo_projects (
  id SERIAL PRIMARY KEY, name TEXT, progress INTEGER, status TEXT,
  assignee TEXT, start_date TEXT, end_date TEXT, budget INTEGER, spent INTEGER
);
CREATE TABLE IF NOT EXISTS kimbiseo_sales (
  id SERIAL PRIMARY KEY, date TEXT, product TEXT, category TEXT,
  quantity INTEGER, unit_price INTEGER, amount INTEGER, region TEXT
);
CREATE TABLE IF NOT EXISTS kimbiseo_diaries (
  id SERIAL PRIMARY KEY, date TEXT, emoji TEXT,
  keywords TEXT[], content TEXT, record TEXT
);

-- RLS 공개 읽기 허용
ALTER TABLE kimbiseo_tasks     ENABLE ROW LEVEL SECURITY;
ALTER TABLE kimbiseo_schedules ENABLE ROW LEVEL SECURITY;
ALTER TABLE kimbiseo_projects  ENABLE ROW LEVEL SECURITY;
ALTER TABLE kimbiseo_sales     ENABLE ROW LEVEL SECURITY;
ALTER TABLE kimbiseo_diaries   ENABLE ROW LEVEL SECURITY;

CREATE POLICY "public read" ON kimbiseo_tasks     FOR SELECT USING (true);
CREATE POLICY "public read" ON kimbiseo_schedules FOR SELECT USING (true);
CREATE POLICY "public read" ON kimbiseo_projects  FOR SELECT USING (true);
CREATE POLICY "public read" ON kimbiseo_sales     FOR SELECT USING (true);
CREATE POLICY "public read" ON kimbiseo_diaries   FOR SELECT USING (true);
```

**dashboard.html에 Supabase 연동 추가:**
```javascript
const SUPA_URL  = 'https://[PROJECT_REF].supabase.co';
const SUPA_ANON = '[ANON_KEY]';  // Supabase 대시보드 → API Keys → anon

async function sbGet(table, qs) {
  const url = SUPA_URL + '/rest/v1/' + table + (qs ? '?' + qs : '');
  const res = await fetch(url, {
    headers: { 'apikey': SUPA_ANON, 'Authorization': 'Bearer ' + SUPA_ANON }
  });
  return res.json();
}

async function loadDashboard() {
  var tasks    = await sbGet('kimbiseo_tasks',    'order=id.asc');
  var projects = await sbGet('kimbiseo_projects', 'order=progress.desc');
  var diaries  = await sbGet('kimbiseo_diaries',  'order=date.desc');
  // ... 렌더링
}
loadDashboard();
```

**API Keys 확인:**
```bash
supabase projects api-keys --project-ref [PROJECT_REF]
# anon 키를 dashboard.html에 사용 (공개 노출 안전)
```

---

### STEP 13 — Vercel 배포

```bash
# 1. 로그인
vercel login   # Continue with GitHub

# 2. 배포 (프로젝트 폴더에서)
vercel --prod

# 또는 GitHub 연동으로 자동 배포:
# vercel.com → Add New Project → GitHub 레포 선택
# Framework: Other (정적 HTML)
# → Deploy
```

Vercel 배포 후 GitHub에 push할 때마다 자동으로 재배포됨.

---

### STEP 14 — CLAUDE.md 작성

**Claude에게:** `"CLAUDE.md 만들어줘. 프로젝트명, 주요 기능, 파일 구조, 사용 기술 포함"`

CLAUDE.md는 Claude Code가 프로젝트를 이해하기 위해 자동으로 읽는 파일.  
새 세션을 열 때마다 프로젝트 컨텍스트를 유지해줌.

---

## 슬래시 커맨드 사용법

| 커맨드 | 동작 |
|--------|------|
| `/김비서` | 데이터 읽기 → 대시보드 업데이트 → 오늘 브리핑 출력 |
| `/배포` | 변경 파일 확인 → 커밋 → GitHub 푸시 → 완료 메시지 |

---

## 핵심 설계 원칙

1. **상대경로만 사용** — `/index.html` ❌ → `index.html` ✅ (Vercel 배포 시 404 방지)
2. **외부 라이브러리 없음** — 순수 HTML/CSS/JS + SVG (빌드 도구 불필요)
3. **anon key는 커밋 가능** — Supabase anon key는 클라이언트 공개용 (RLS로 보호)
4. **데이터는 gitignore** — CSV, TXT, 일기 파일은 GitHub에 올리지 않음
5. **FOUC 방지** — 다크모드 초기화 스크립트는 `<head>` 최상단에 배치

---

## 자주 쓰는 명령어

```bash
# 로그인 상태 확인
gh auth status
supabase projects list
vercel whoami

# 배포
git add -A && git commit -m "메시지" && git push

# Supabase 프로젝트 연결
supabase link --project-ref [REF]

# API 키 확인
supabase projects api-keys --project-ref [REF]
```

---

## 트러블슈팅

| 문제 | 해결 |
|------|------|
| Vercel 404 | 링크에 `/` 제거 (절대경로 → 상대경로) |
| 다크모드 깜빡임 | theme 스크립트를 `<head>` 최상단으로 이동 |
| Supabase IPv6 오류 | 대시보드 SQL 에디터에서 직접 실행 |
| git rm --cached 후 재추가 | `git add -A` 전에 .gitignore 먼저 확인 |
| JS 구문 오류 (개행 문자) | Python f-string에서 `\n` → `\\n` 으로 이스케이프 |
| supabase db push 실패 | 네트워크 IPv6 미지원 → Management API 또는 대시보드 SQL 에디터 사용 |

---

*작성일: 2026-06-07 · Claude Code 워크숍*
