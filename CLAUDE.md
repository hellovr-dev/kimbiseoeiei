# 김비서 (KimBiseo)

AI 기반 업무 비서 대시보드. 은행 연동 없이 구독·일정·프로젝트·매출을 한 화면에서 관리한다.

---

## 주요 기능

| 기능 | 설명 |
|------|------|
| **업무 대시보드** | 할 일 목록(체크박스·우선순위), 주간 일정표, 프로젝트 진행률, 매출 요약을 2×2 카드 레이아웃으로 표시 |
| **매출 차트** | 거래일별 선 그래프 + 제품별 막대 그래프. 외부 라이브러리 없이 순수 SVG로 구현 |
| **회의록 요약** | 회의 기본 정보·논의 요약·액션 아이템 표·다음 회의 안내. 인쇄/PDF 저장 지원 |
| **업무 프로세스 다이어그램** | 기획→제작→검토→배포→분석 5단계 SVG 다이어그램 |
| **사이트 분석 보고서** | subtrac.kr 구조·디자인·잘한점/개선점 분석 카드 |
| **다크/라이트 모드** | 모든 페이지 공통 토글. `localStorage('kim-theme')`으로 상태 유지 |
| **페이지 간 네비게이션** | 상단 탭 메뉴(sticky). 현재 페이지 자동 강조(`location.pathname` 기반) |
| **`/김비서` 슬래시 커맨드** | 데이터 읽기 → dashboard.html 동기화 → 오늘의 브리핑 출력 |

---

## 파일 구조

```
/
├── index.html              # 소개 메인 페이지 (글래스모피즘, CTA 버튼)
├── dashboard.html          # 업무 대시보드 (핵심 페이지)
├── chart.html              # 매출 데이터 시각화
├── diagram.html            # 업무 프로세스 래퍼 페이지
├── diagram.svg             # 업무 프로세스 다이어그램 원본
├── meeting-result.html     # 회의록 요약
├── report.html             # 사이트 분석 보고서
├── CLAUDE.md               # 프로젝트 문서 (이 파일)
├── .gitignore
│
├── 김비서-데이터/          # 원본 데이터 파일
│   ├── 업무목록.csv         # 업무·우선순위·상태·담당자·마감일
│   ├── 주간일정.txt         # 주간 일정 (월~금)
│   ├── 프로젝트현황.csv     # 프로젝트명·진행률·예산·집행액
│   ├── 매출데이터.csv       # 거래일·제품·수량·단가·매출액·지역
│   └── 회의록.txt           # 마케팅팀 주간회의 회의록
│
├── 정리해줘/               # 문서 정리 예시 폴더
│   ├── 보고서/
│   ├── 메모/
│   ├── 업무/
│   └── 기타/
│
└── .claude/
    └── commands/
        └── 김비서.md        # /김비서 슬래시 커맨드 정의
```

---

## 사용 기술

| 분류 | 기술 |
|------|------|
| **언어** | HTML5, CSS3, Vanilla JavaScript (빌드 도구 없음) |
| **차트** | 순수 SVG — 외부 라이브러리 미사용 |
| **디자인** | 글래스모피즘 (`backdrop-filter: blur`), CSS 변수로 다크/라이트 테마 전환 |
| **배포** | GitHub → Vercel (정적 사이트, 프레임워크 없음) |
| **데이터** | CSV / TXT 파일 → JS 배열로 하드코딩 (백엔드 없음) |
| **AI 도구** | Claude Code (코드 생성·분석·슬래시 커맨드) |

---

## 페이지 연결 구조

```
index.html
    └──▶ dashboard.html (탭 네비게이션)
              ├──▶ meeting-result.html
              ├──▶ chart.html
              ├──▶ diagram.html ──▶ diagram.svg
              └──▶ report.html
```

---

## 슬래시 커맨드

| 커맨드 | 동작 |
|--------|------|
| `/김비서` | 데이터 파일 전체 읽기 → dashboard.html 동기화 → 오늘 브리핑 출력 |

커맨드 정의 파일: `.claude/commands/김비서.md`

---

## 개발 노트

- 모든 링크·경로는 **상대경로** 사용 (`/` 로 시작하지 않음) — Vercel 배포 시 404 방지
- 다크/라이트 토글은 페이지 로드 직후 `<head>` 에서 테마 클래스를 적용해 **FOUC 방지**
- SVG 차트는 `window.resize` 이벤트로 **반응형 리렌더링** 지원
- 인쇄 레이아웃(`@media print`)은 meeting-result.html에만 적용
